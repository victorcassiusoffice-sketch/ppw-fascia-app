{
  "name": "ppw-push-sender",
  "version": "0.1.0",
  "private": true,
  "description": "PPW Fascia App Web Push sender Worker — [VIC-SETUP] free Cloudflare Worker cron. Not part of the app build.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
